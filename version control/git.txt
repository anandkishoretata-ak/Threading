project.doc
project1.doc---?
project2.doc-->3rd

which file is newent.

who changed what?

delete --we can recover the code

multiple people can work together

Linus Torvalds--->2005

Git Architecture

                    working directory:hello.py ver-1,ver-2
                        |
                    staging area:Temporary area before commit
                        |
                    local   Repository:area after commit(within system)
                        |
                    remote Repository(GitHub,gitlab cloud)

                                     


       create a file
          |
       git add
          |
        staging area
          |
        git commit
          |
       local Repository
          |
       git push
          |
       cloud(remote Repository)                                     